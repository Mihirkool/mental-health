services:
  - type: web
    name: mental-health-agent
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app" # This has been updated to point to app.py
    envVars:
      - key: PORT
        value: "8000"